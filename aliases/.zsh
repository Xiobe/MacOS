# The default shell on MacOS is zsh and not bash this is why this file is called .zsh instead of .bash_profile.
# The file is also located in $HOME. To load the aliases it is just like in Linux 'source <file>'.

# redeclare ls to work with a timestamp that shows the year too.
alias ls='ls -lT'
